# ── Python ──────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
wheels/

# ── uv / venv ───────────────────────────────────
.venv/
venv/
env/
.python-version-local

# ── Type-checker / test caches ─────────────────
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
coverage.xml

# ── IDE ─────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ── Secrets & runtime state ────────────────────
.env
.env.local
.env.*.local
keys.py
sales_tracking.db
*.db
*.db-journal
*.db-wal
*.db-shm

# ── ngrok / tunnel runtime ─────────────────────
ngrok.log
*.log

# ── Piper TTS models (large ONNX binaries) ───────
models/

# ── Demo transcripts (may contain PII) ───────────
transcripts/

# ── Audio scratch / generated samples ───────────
ambient_noise.wav
assets/*.wav
assets/*.m4a
!assets/.gitkeep

# ── Local binaries (ngrok etc.) ─────────────────
bin/

# ── OS ──────────────────────────────────────────
Thumbs.db
